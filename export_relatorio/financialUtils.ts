/**
 * Adaptador fino — SSOT oficial: lib/financialUtils.ts
 *
 * Classificação P1-03: (B) adaptador legítimo de exportação.
 * A cópia histórica divergia do motor principal; este arquivo reexporta a fonte única.
 */
export * from '../lib/financialUtils';
